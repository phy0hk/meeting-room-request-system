import ErrorModal from "./error-modal";
import LogoutModal from "./logout-dialog";

const Dialog = () => {
    return (
        <>
            <ErrorModal />
            <LogoutModal />
        </>
    );
};
export default Dialog;
